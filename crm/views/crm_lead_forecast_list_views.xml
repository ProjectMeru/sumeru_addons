<?xml version="1.0" encoding="utf-8"?>
<sumeru>
    <data>
        <view id="view_crm_lead_forecast_list" model="crm.lead" type="list">
            <field name="name" string="Opportunity"/>
            <field name="partner_id" string="Customer"/>
            <field name="date_deadline" string="Expected Closing"/>
            <field name="expected_revenue" string="Expected Revenue"/>
            <field name="prorated_revenue" string="Prorated Revenue"/>
            <field name="recurring_revenue" string="Recurring Revenue"/>
            <field name="probability" string="Probability"/>
            <field name="stage_id" string="Stage"/>
            <field name="user_id" string="Salesperson"/>
        </view>
    </data>
</sumeru>
