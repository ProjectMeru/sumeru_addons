<?xml version="1.0" encoding="utf-8"?>
<sumeru>
    <data>
        <view id="view_crm_lead_pipeline_kanban" model="crm.lead" type="kanban" priority="20" default_group_by="stage_id" records_draggable="1">
            <field name="stage_id"/>
            <field name="name"/>
            <field name="partner_id"/>
            <field name="contact_name"/>
            <field name="expected_revenue"/>
            <field name="recurring_revenue"/>
            <field name="priority" widget="priority"/>
            <field name="user_id"/>
            <field name="date_deadline"/>
        </view>
    </data>
</sumeru>
