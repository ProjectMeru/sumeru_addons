// Package models contains models for the contacts module.
package models

// No new models needed; contacts module uses base's core.partner

func init() {
	// No new models to register; core.partner is already registered in base
}
