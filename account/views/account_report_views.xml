<?xml version="1.0" encoding="utf-8"?>
<sumeru>
    <data>
        <view id="view_account_invoice_report_tree" model="account.invoice.report" type="tree" open="0">
            <field name="name" string="Number"/>
            <field name="partner_id" string="Partner"/>
            <field name="move_type" string="Type"/>
            <field name="invoice_date" string="Date"/>
            <field name="state" string="Status"/>
            <field name="payment_state" string="Payment"/>
            <field name="amount_untaxed" string="Untaxed"/>
            <field name="amount_tax" string="Tax"/>
            <field name="amount_total" string="Total"/>
            <field name="amount_residual" string="Residual"/>
        </view>
        <record id="action_account_invoice_report" model="sys.action.window">
            <field name="name">Invoice Analysis</field>
            <field name="core_model">account.invoice.report</field>
            <field name="view_mode">tree</field>
        </record>
    </data>
</sumeru>
